## Singleton Classes
- a singleton class is one which allows only one object to be created, like it can only have one instance.
- in logical sense, you have to stop people from calling the constructor --> so i make it private.